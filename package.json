{
  "name": "gps-nav-aid",
  "private": true,
  "description": "단일 페이지 HTML5 GPS 보조 항법장치 (GitHub Pages 배포). 이 package.json 은 테스트용 개발 의존성만 담는다 — 앱 자체는 빌드가 없다.",
  "scripts": {
    "test": "node tests/run.mjs",
    "check": "node tests/check-syntax.mjs"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  }
}
